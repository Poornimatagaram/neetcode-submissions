class Solution:
    def calPoints(self, operations: List[str]) -> int:
        record_output = []
        for i in range(len(operations)):
            if i == int:
                record_output.append(x)
            elif i == "+":
                record_output.sum()

            elif i == "C":
                record_output.pop()

            elif i =="D":
                record_output.append(range(len()))

            else:
                record_output    

        return record_output

finPoints = Solution()
print(finPoints.calPoints)
                
        